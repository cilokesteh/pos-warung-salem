rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users to read/write all data
    // (single-admin app, not for multi-user production)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}